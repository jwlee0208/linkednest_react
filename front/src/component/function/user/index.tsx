export const inputUsernameVal = (e: React.ChangeEvent<HTMLInputElement>) => {
    e.preventDefault();
    return false;
}

export const inputNicknameVal = (e: React.ChangeEvent<HTMLInputElement>) => {
    e.preventDefault();
    return false;
}

export const inputEmailVal = (e: React.ChangeEvent<HTMLInputElement>) => {
    e.preventDefault();
    return false;
}
